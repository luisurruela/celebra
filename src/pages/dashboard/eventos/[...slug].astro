---
import Layout from "../../../layouts/Layout.astro";
import EventDetails from "../../../components/EventDetails.jsx";

const eventId = Astro.params.slug;
---

<Layout title="Detalles del Evento | Celebra" description='Detalles del evento seleccionado en Celebra'>
  <EventDetails client:only="react" eventId={eventId} />
</Layout>
